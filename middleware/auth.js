/* var isLoggedIn = (req, res, next) => {
  if (req.isAuthenticated()) {
    return next();
  }
  req.flash('error', '¡Debes estar registrado para hacer eso!');
  res.redirect('/ingresar');
};
*/
